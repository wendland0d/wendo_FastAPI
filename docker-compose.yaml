version: '3.9'

services:
  
  db:
    image: postgres:latest
    restart: always
    container_name: fast_db
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=kefir_api
      - POSTGRES_PORT=5432
    ports:
      - 5432:5432